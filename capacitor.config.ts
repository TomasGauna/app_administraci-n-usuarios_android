import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'administracion.usuarios',
  appName: 'Administracion de Usuarios',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
